# Copy to .env (gitignored) and fill in. promptfoo reads this automatically.
ANTHROPIC_API_KEY=sk-ant-...

# Opik tracing (optional — unset = local self-hosted at localhost:5173, no key needed)
OPIK_URL_OVERRIDE=
OPIK_API_KEY=
OPIK_WORKSPACE=
